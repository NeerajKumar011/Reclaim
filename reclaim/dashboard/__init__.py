"""Dashboard — web UI for monitoring recovery operations.

TODO: Future phase — Implement:
  - Real-time recovery dashboard (live metrics, event stream)
  - Customer timeline view (full history of events, actions, outcomes)
  - Manual intervention controls (override state, pause recovery, opt-out)
  - Revenue recovery metrics (charts, trends, cohort analysis)
  - Audit log viewer (searchable, filterable)
  - Alert configuration (notify on anomalies, high failure rates)
"""
