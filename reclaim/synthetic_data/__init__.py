"""Synthetic data generator — creates realistic test scenarios.

TODO: Phase 2 — Implement:
  - Razorpay-shaped test payload generation for all event types
  - Realistic failure scenario sequences (failed → retry → captured)
  - checkout_abandoned payload generation (not a native Razorpay event)
  - invoice_overdue payload generation (not a native Razorpay event)
  - Configurable customer profiles (language, channel preferences)
  - Time-series generation (realistic event timing patterns)
  - Bulk scenario generation for load testing
"""
